<%= form_for(@bill_friend, :url => bill_bill_friends_path(@bill)) do |f| %>
  <% if @bill_friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill_friend.errors.count, "error") %> prohibited this bill_friend from being saved:</h2>

      <ul>
      <% @bill_friend.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :bill_id, :value => @bill.id%>

  <div class='field'>
    <%= f.label :user %>
    <%= f.select :user_id, User.all.collect{|u| [u.name, u.id]} %>
  </div>

  <div class='field'>
    <%= f.label :contribution %>
    <%= f.number_field :contribution %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
