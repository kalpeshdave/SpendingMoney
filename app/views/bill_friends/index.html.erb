<h1>Listing bill_friends</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @bill_friends.each do |bill_friend| %>
  <tr>
    <td><%= link_to 'Show', bill_friend %></td>
    <td><%= link_to 'Edit', edit_bill_friend_path(bill_friend) %></td>
    <td><%= link_to 'Destroy', bill_friend, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bill friend', new_bill_friend_path %>
